import 'package:flutter/material.dart';

class FiMoutasim extends StatelessWidget {
  const FiMoutasim({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: const Text("Moutasim"),
      ),
      body: const Center(
        child: Text("Moutasim name"),
      ),
    );
  }
}
